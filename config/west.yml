manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: sekigon-gonnoc
      url-base: https://github.com/sekigon-gonnoc
    - name: kot149
      url-base: https://github.com/kot149
    - name: cormoran
      url-base: https://github.com/cormoran
  projects:
    # DYA Studio(Level 2)対応のため、公式ZMK v0.3ではなくcormoran氏のフォーク
    # （v0.3タグ + Custom Studio Protocol + BLE管理拡張の追加的パッチ）を使用。
    # Zephyrのバージョンは公式v0.3と同一(v3.5.0+zmk-fixes)で、split/BLE/studio関連の
    # 追加のみのためドライバ・他社製モジュールへの影響は小さいと判断（詳細はmemos参照）。
    - name: zmk
      remote: cormoran
      revision: v0.3-branch+custom-studio-protocol+ble
      import: app/west.yml
    # DYA Studio Level 2: カスタム設定の永続化バックエンド（他モジュールの依存先）。
    # mainブランチHEAD相当のコードは zephyr_linker_sources(ROM_SECTIONS ...) を使うが、
    # このキーボードが使うZephyr v3.5.0+zmk-fixes（v0.3系）には ROM_SECTIONS という
    # linker snippet位置が存在せず（DATA_SECTIONS/RODATA等のみ）ビルド不能。
    # この機能が導入される前のコミットに固定している（詳細はmemos/dya-studio-integration.md）。
    - name: zmk-feature-custom-settings
      remote: cormoran
      revision: 5be86dcfb903ecc12624b02cadb40bbdbc78abe3
    # DYA Studio Level 2: トラックボール/トラックパッド速度をWeb UIから実行時調整
    - name: zmk-module-runtime-input-processor
      remote: cormoran
      revision: main
    - name: zmk-component-bmp-boost
      remote: sekigon-gonnoc
      import: west.yml
      revision: v0.2
    - name: zmk-feature-status-led
      remote: sekigon-gonnoc
      revision: main
    - name: zmk-driver-paw3222
      remote: sekigon-gonnoc
      revision: torabo-tsuki
    - name: zmk-driver-iqs7211e
      remote: sekigon-gonnoc
    - name: zmk-feature-cdc-acm-bootloader-trigger
      remote: sekigon-gonnoc
      revision: v0.2
    - name: zmk-feature-non-lipo-battery-management
      remote: sekigon-gonnoc
      revision: main
    - name: zmk-scroll-snap
      remote: kot149
      revision: v1
    - name: zmk-layout-shift
      remote: kot149
      revision: v2
  self:
    path: config
